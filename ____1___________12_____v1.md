# フェーズ1 公開手順書（プログラミング未経験の方向け）

この手順書だけで、**12カ国のニュースを毎日自動で集め、日本語に直し、同じ話題ごとに束ねて「世界TOP30／日本TOP30」として公開する**ところまで到達できます。

- **ターミナル（黒い画面）は一度も使いません。**
- **gitコマンドも打ちません。** すべてGitHubのWebサイト上のボタン操作です。
- **お金はかかりません。**（GitHubの無料枠 ＋ Google AI Studioの無料枠だけで完結します）
- **`pip install` も不要です。** 3つのスクリプトは標準ライブラリだけで書かれています。
- **AIのキーを登録しなくても、サイトは動きます。**（ただし機能が一部落ちます。詳しくは STEP 7）

> **所要時間の目安**：30〜50分。うちAPIキーの取得が5分ほどです。

---

## この手順書で作られるもの

| 見えるもの | 中身 |
| --- | --- |
| カレンダー | 日付を押すとその日の話題一覧に切り替わります |
| **世界TOP30** | その日、**世界で最も多くの国・媒体が報じた話題**の上位30件 |
| **日本TOP30** | その日、**日本の媒体が報じた話題**の上位30件 |
| 各話題の中身 | 日本語の見出し／事実要約／タグ／**論調（立場）ラベル**／報じた国と媒体の一覧 |

話題の順位は **「報じた国の数 × 3 ＋ 掲載媒体の数」** で決めています。「1カ国が大量に書いている話題」より「多くの国がそろって書いている話題」が上に来る計算です。

### 実測済みの動作実績（この手順書を書いた環境での確認結果）

| 項目 | 実測値 |
| --- | --- |
| 対象国 | **12カ国** |
| 採用フィード | **27本** |
| 取得できた実記事 | **740件** |
| 生成された話題 | **524件** |
| うち**複数の国**が報じた話題 | **28件** |
| 自動テスト | `test_fetch` / `test_enrich` / `test_cluster` の**3本ともOK** |
| 記事本文の保存 | **本文系キー0件**（著作権配慮。見出しとリンクのみ） |
| 再実行しても壊れないか | **冪等**（同じ日に2回動かしてもバイト単位で同一の結果） |
| スマホ表示 | **幅375pxで横スクロールなし** |

12カ国は **日本・アメリカ・イギリス・フランス・ドイツ・ロシア・中国・韓国・インド・ブラジル・ウクライナ・カタール** です。

---

## 全体の流れ（地図）

先に全体像をつかんでください。迷子になったらここに戻ります。

```
STEP 0  手元のファイルを確認する                （5分）
STEP 1  GitHubアカウントを作る                  （5分・持っていれば飛ばす）
STEP 2  リポジトリ（ファイルの置き場）を作る     （3分）
STEP 3  ファイルをブラウザからアップロードする   （10分）★つまずきやすい
STEP 4  Actions（自動実行）を有効にする          （1分）
STEP 5  Actionsに「書き込み権限」を与える        （2分）★必須
STEP 6  Pages（公開）の設定をする                （3分）
STEP 7  GEMINI_API_KEY を登録する               （10分）★重要
STEP 8  初回を手動で実行する（Run workflow）     （5分）
STEP 9  公開URLを開いて確認する                  （2分）
```

毎日の中身はこう動きます。**あなたは何も操作しません。**

```
毎朝7:10ごろ（日本時間）
  ①収集   scripts/fetch.py    → data/days/（日付）.json          12カ国の見出しを集める
  ②AI付与 scripts/enrich.py   → data/days/（日付）.enriched.json  日本語訳・要約・タグ・論調
  ③クラスタ scripts/cluster.py → data/topics/（日付）.json         同じ話題を束ねて採点
  ④表示   index.html が③を読むだけ（サーバー不要）
```

---

## STEP 0：手元のファイルを確認する

パソコンの `phase1` フォルダを開いて、**次の4つがそろっている**ことを確認してください。これがアップロードする本体です。

| 名前 | 種類 | 役割 |
| --- | --- | --- |
| `index.html` | ファイル | サイトの見た目そのもの。これ1枚で全画面を描きます |
| `feeds.json` | ファイル | **どの国のどの媒体を読むかの一覧表**。フィードが死んだときはここを直します |
| `scripts` | フォルダ | `fetch.py` `enrich.py` `cluster.py` とテスト3本が入っています |
| `.github` | フォルダ | 中に `workflows` → `daily.yml`。**毎日動かす時刻と手順**が書かれています |

`sample-data` フォルダもあれば一緒に上げておくと、ネットが無い環境でも見た目の確認ができます（任意・トラブルシュートEで使います）。

> **`data` フォルダは上げなくて大丈夫です。** STEP 8 の初回実行でGitHubが自動的に作ります。手元の `data` は開発中の確認用データなので、上げても上げなくても構いません。

### 確認しておくこと

1. `.github` フォルダの中を開いて、`workflows` フォルダ → その中に `daily.yml` があるか。
2. `scripts` フォルダの中に `fetch.py` `enrich.py` `cluster.py` の**3つ**があるか。
3. `feeds.json` を「メモ帳」や「テキストエディット」で開けるか（STEP 3とトラブルシュートで中身を見ます）。

> `.github` が見えない場合、パソコンが「ドットで始まるフォルダ」を隠しています。
> - **Windows**：エクスプローラーの「表示」タブ →「隠しファイル」にチェック
> - **Mac**：Finderで `command` + `shift` + `.`（ピリオド）を押す

---

## STEP 1：GitHubアカウントを作る

すでにお持ちの方は **STEP 2** へ進んでください。

1. ブラウザで `https://github.com/` を開きます。
2. 右上の **「Sign up」** ボタンを押します。
3. メールアドレス・パスワード・ユーザー名を入力します。
   - **ユーザー名はサイトのURLに入ります。** 例：ユーザー名が `taro` なら公開URLは `https://taro.github.io/...` になります。短めの英数字がおすすめです。
4. 画面の指示どおりに進み、届いたメールの**確認コード**を入力します。
5. プランを選ぶ画面が出たら **「Free」**（無料）を選びます。

> **クレジットカードの登録は不要です。** 求められる画面が出たら、それは有料プランの案内です。戻って「Free」を選び直してください。

---

## STEP 2：リポジトリ（置き場）を作る

「リポジトリ」とは、ファイルを置く箱のことです。

1. GitHubにログインした状態で、右上の **「+」** マークを押し、**「New repository」** を選びます。
2. **「Repository name」** の欄に名前を入れます。この手順書では **`world-lens`** として説明します。
   - 半角英数字とハイフンだけにしてください。日本語やスペースは使えません。
3. **「Description」**（説明）は空欄でOKです。
4. **★重要★** その下の **「Public」**（公開）を選びます。**「Private」では公開できません。**
5. **「Add a README file」のチェックは外したまま**にします（入れると後の操作が少し増えます）。
6. 一番下の緑の **「Create repository」** ボタンを押します。

### なぜ「Public」にしないといけないのか

GitHub Pages（サイトの公開機能）は、**無料プランでは Public のリポジトリでしか使えません。** Private のままだと STEP 6 で公開設定の画面に進めず、「Upgrade」（有料プランへの案内）が出ます。

**Public で公開されるのは、あなたがアップロードしたファイルとニュースの見出しだけです。** メールアドレスやパスワードが公開されることはありません。**APIキーも公開されません**（STEP 7 で「Secrets」という専用の隠し場所に入れるためです）。

---

## STEP 3：ファイルをブラウザからアップロードする

ここが一番つまずきやすい場所です。**フォルダごと**アップロードするのがコツです。

### 3-1. まず `index.html` と `feeds.json` と `scripts` を上げる

1. リポジトリのトップ画面で **「Add file」** ボタン → **「Upload files」** を選びます。
2. 別ウィンドウでパソコンの `phase1` フォルダを開きます。
3. `phase1` の中身のうち、**`index.html` / `feeds.json` / `scripts`（と、あれば `sample-data`）を選び**（`.github` 以外）、ブラウザの **「Drag files here ...」** と書かれた枠の中へ**ドラッグ＆ドロップ**します。
   - **フォルダはそのままドラッグしてOKです。** フォルダ構成は保持されます。
   - うまくいくと、枠の下に `scripts/fetch.py` のように**スラッシュ付きのパス**でファイル名が並びます。ここが `fetch.py` だけになっていたらフォルダ構成が失われているので、いったんページを再読み込みしてフォルダごとドラッグし直してください。
4. 下の **「Commit changes」** ボタンを押します（メッセージ欄は空欄のままでOK）。

### 3-2. 次に `.github` フォルダを上げる

`.github` はドットで始まるためドラッグに失敗しやすいので、**確実な「手入力」方式**で作ります。

1. リポジトリのトップ画面で **「Add file」** → **「Create new file」** を選びます。
2. ファイル名の入力欄（`Name your file...`）に、**次の文字列をそのまま貼り付けます**。

```
.github/workflows/daily.yml
```

> **ポイント**：`/`（スラッシュ）を打つと、GitHubが自動でフォルダとして扱ってくれます。これでフォルダを作らずに深い階層のファイルが作れます。

3. その下の大きな入力エリアに、手元の `daily.yml` の中身を**全部コピーして貼り付けます**。
   - 手元の `daily.yml` は「メモ帳」や「テキストエディット」で開けます。
   - **1行目から最後の行まで、まるごと**貼ってください。先頭の `#` で始まる説明行も消さないでください。
4. 右上の **「Commit changes...」** ボタン → 出てきたダイアログでもう一度 **「Commit changes」** を押します。

### 3-3. アップロードできたか確認する

リポジトリのトップ画面で、次のように表示されていればOKです。

- `.github` フォルダがある
- `scripts` フォルダがある
- `index.html` がある
- **`feeds.json` がある**

さらに念のため：

- `scripts` を押して、中に **`fetch.py` `enrich.py` `cluster.py`** の3つがあるか
- `.github` → `workflows` の中に **`daily.yml`** があるか

> **`feeds.json` が抜けていると STEP 8 で必ず失敗します。**「どの媒体を読むか」の一覧表なので、これが無いと `fetch.py` は1件も取得できません。トップ画面に見えているか、もう一度確認してください。

---

## STEP 4：Actions（自動実行）を有効にする

1. リポジトリ上部のメニューから **「Actions」** タブを押します。
2. **「I understand my workflows, go ahead and enable them」** という緑のボタンが出た場合は、**そのボタンを押します**。
3. 左側に **「daily-pipeline」** という名前が出てくればOKです。

> 何も出ない場合は `.github/workflows/daily.yml` の置き場所が間違っています。STEP 3-2 をやり直してください。ファイル名が `.github/workflows/daily.yml` に**完全に一致**している必要があります（`github/workflows/daily.yml` のようにドットが抜けていると認識されません）。

---

## STEP 5：Actionsに「書き込み権限」を与える ★必須★

これを飛ばすと、**STEP 8で必ずエラーになります。** 集めたデータをリポジトリに保存し直す作業に、書き込み権限が必要です。

1. リポジトリ上部の **「Settings」**（歯車マーク）タブを押します。
2. 左メニューを下にスクロールし、**「Actions」** を押して、その下に出る **「General」** を押します。
3. ページを一番下までスクロールし、**「Workflow permissions」** という見出しを探します。
4. **「Read and write permissions」** のラジオボタン（丸いボタン）を選びます。
   - 最初は「Read repository contents and packages permissions」が選ばれています。それを**書き込みありに変更**します。
5. すぐ下の **「Save」** ボタンを押します。

---

## STEP 6：Pages（公開）の設定をする

1. リポジトリ上部の **「Settings」** タブを押します。
2. 左メニューの **「Pages」** を押します。
3. **「Build and deployment」** の下、**「Source」** が **「Deploy from a branch」** になっていることを確認します（なっていなければ選び直します）。
4. その下の **「Branch」** で：
   - 左のドロップダウン：**`main`** を選ぶ
   - 右のドロップダウン：**`/ (root)`** を選ぶ
5. **「Save」** ボタンを押します。
6. 画面上部に
   **「Your site is live at https://（ユーザー名）.github.io/world-lens/」**
   のような表示が出るまで、**1〜3分待ってページを再読み込み**します。

> この時点でURLを開くと「データがまだありません」と出ます。**正常です。** データはSTEP 8で作られます。

---

## STEP 7：GEMINI_API_KEY を登録する ★重要★

ここが**フェーズ1で新しく増えた作業**です。AI（Google Gemini）に「日本語への翻訳・事実要約・タグ付け・論調の判定」をやらせるための鍵（キー）を登録します。

### 7-0. 先に読んでください：登録しなくてもサイトは動きます

**キーを登録しなくても、STEP 8 は緑のチェックで成功し、サイトは公開されます。** ただし機能が落ちた状態（これを **degraded** と呼びます）になります。何がどう違うのかを、先にはっきり示します。

| | **キーを登録した場合** | **キーを登録しない場合（degraded）** |
| --- | --- | --- |
| 見出しの言語 | **日本語に翻訳される** | **原語のまま**（英語・中国語・ロシア語・アラビア語・ポルトガル語などが混ざって並びます） |
| 事実要約 | 数行の要約が付く | **空欄** |
| タグ | 付く | **空** |
| 論調（立場）ラベル | 「支持／批判／中立」などが付く | **すべて「言及なし」** |
| 話題の束ね方 | **国をまたいで束なる**（例：NHKの「〜首脳会談」とBBCの "... summit" が同じ話題に入る） | **同じ言語同士しか束ならない** |
| 世界TOP30 | 複数国が報じた話題が上位に並ぶ | **ほぼ1カ国だけの話題が並ぶ**（順位付けの意味が薄れます） |
| 日本TOP30 | 日本の報じ方と海外の報じ方を比べられる | 日本の記事だけが孤立し、**比較できません** |
| 画面上部の表示 | 何も出ません | **「⚠ この日のデータにはAI処理が適用されていません（degraded）。」** という警告帯が出ます |

**なぜ束ねられなくなるのか**：話題の束ね（クラスタ）は**見出しの文字の重なり**で「同じ話題かどうか」を判定しています。日本語の「首脳会談」と英語の "summit" は**文字が1つも重なりません**。だから翻訳が無いと、日本の記事と海外の記事は永久に別の話題として扱われます。**「世界と日本を比べる」というこのサイトの一番の目的が働かない**、ということです。

> つまり、**キー無しでも「12カ国のニュースが日付ごとに並ぶサイト」にはなりますが、「比較サイト」にはなりません。** 比較をしたい方は、以下の7-1から進めてください。

### 7-1. Google AI Studio でキーを取得する

1. ブラウザで **`https://aistudio.google.com/apikey`** を開きます。
2. Googleアカウントでのログインを求められたらログインします（GitHubのアカウントとは別物です。普段お使いのGoogleアカウントで構いません）。
3. 利用規約の同意画面が出たら、内容を読んでチェックを入れ、進みます。
4. **「Create API key」** ボタンを押します。
   - 「Search Google Cloud projects」のようにプロジェクトを選ぶ欄が出た場合は、**新規プロジェクトを作る選択肢**（`Create API key in new project` など）を選べば大丈夫です。
5. `AIza...` で始まる**長い文字列**が表示されます。これがAPIキーです。
6. **「Copy」** ボタン（コピーのアイコン）を押して、クリップボードにコピーします。

> **クレジットカードの登録は不要です。** 無料枠のまま使えます。カード情報を求める画面が出たら、それは有料の「Pay-as-you-go」への案内です。登録せずに戻ってください。

#### ★取得したキーの扱いについて、必ず守ること★

- **キーをコードに直書きしないでください。** `daily.yml` や `enrich.py` の中に `AIza...` をそのまま貼り付けては**絶対にいけません**。このリポジトリは Public（公開）なので、**世界中の誰からでも読めてしまいます**。
- **キーを他人に見せないでください。** スクリーンショット、SNS、チャット、ブログ、質問サイトへの貼り付けもすべて同じ危険があります。
- 正しい置き場所は、次の 7-2 で使う **「Secrets」** という**GitHub専用の隠し金庫**だけです。ここに入れたキーは、**登録した本人でも二度と画面に表示されません**（Actionsのログにも出ません）。
- **もし誤って公開してしまったら**：`https://aistudio.google.com/apikey` に戻り、そのキーを削除（Delete）して、**新しいキーを作り直してください**。古いキーは無効になります。

### 7-2. GitHubの Secrets に登録する

コピーしたキーを、**GitHubの隠し金庫**に入れます。

1. **あなたのリポジトリ**の画面に戻ります。
2. 上部の **「Settings」** タブを押します。
3. 左メニューを下にスクロールし、**「Secrets and variables」** を押します。
4. その下に出てくる **「Actions」** を押します。
5. 右上の緑のボタン **「New repository secret」** を押します。
6. **「Name」**（名前）の欄に、**次の文字列を正確に入力します**。

```
GEMINI_API_KEY
```

   - **すべて半角の大文字**です。アンダースコア（`_`）は2つ。
   - **前後にスペースを入れないでください。**
   - `GEMINI_APIKEY` や `Gemini_Api_Key` では**動きません**（名前が1文字でも違うと、プログラムは「キーが無い」と判断して degraded になります）。
7. **「Secret」** の欄に、7-1でコピーした `AIza...` のキーを**貼り付けます**。
   - 貼り付けた後、**前後に余分なスペースや改行が入っていないか**確認してください。
8. 緑の **「Add secret」** ボタンを押します。

### 7-3. 登録できたか確認する

同じ画面（Settings → Secrets and variables → Actions）の **「Repository secrets」** の一覧に、

```
GEMINI_API_KEY        Updated now
```

のように**名前だけ**が並んでいればOKです。

> **キーの中身（`AIza...`）は表示されません。これが正常です。** GitHubは登録後、誰にも中身を見せない仕組みになっています。あなた自身も見られません。中身を確認したくなったら、確認するのではなく **7-1でキーを作り直して 7-2 で上書き登録**してください（同じ名前で `New repository secret` を押すか、一覧の右の鉛筆マークから更新できます）。

### 7-4. 無料枠についての注意（必ず読んでください）

このサイトは、無料枠の中で完走するように**リクエスト数をあらかじめ絞って**作られています。

| 項目 | 内容 |
| --- | --- |
| クレジットカード | **不要** |
| 1日に必要なリクエスト数 | **24リクエスト** |
| 無料枠の1日の上限 | **25リクエスト** |
| 余裕 | **1リクエスト**（意図的にギリギリまで使い切る設計です） |

**★警告：無料枠を使う条件★**

Google AI Studio の無料枠には、**「送信した入力がGoogleのモデル改善（学習）に使われ、人間のレビュー担当者が内容を読む場合がある」** という条件があります。これは無料で使うことの対価であり、避けられません。

- このサイトが送っているのは **報道各社が公開しているニュースの見出し**だけです。記事本文も、あなたの個人情報も送っていません（**本文系キーは0件**であることを確認済みです）。
- ですが、この仕組みを改造して**あなた自身の文章や、他人の非公開情報を送るような使い方は避けてください。** 人間に読まれる前提の窓口です。
- 業務上の機密を扱う目的では、無料枠を使ってはいけません。

**リクエスト数についての注意**

- 余裕が**1リクエストしかない**ため、同じ日に**手動実行（Run workflow）を何度も押すと上限に達します**。
- 上限に達すると、その日のAI付与は途中で止まり、**その日は degraded として公開されます**（サイトが壊れるわけではありません）。
- **翌日には枠がリセットされ、自動的に元に戻ります。** 何もしなくて大丈夫です。
- したがって **手動実行は1日1回までに**してください。動作確認は STEP 8 の1回で十分です。

---

## STEP 8：初回を手動で実行する（Run workflow）

毎日の自動実行を待たず、今すぐ動かして確認します。

1. **「Actions」** タブを押します。
2. 左側の **「daily-pipeline」** を押します。
3. 右側に **「This workflow has a workflow_dispatch event trigger.」** と書かれた帯が出ます。その中の **「Run workflow」** ボタンを押します。
4. 小さなメニューが開くので、緑の **「Run workflow」** ボタンをもう一度押します。
5. 5秒ほど待ってページを**再読み込み**すると、一覧に黄色い丸（実行中）の行が現れます。
6. その行を押し、**「pipeline」** を押すと進行状況が見えます。
7. **緑のチェックマーク（✓）**になれば成功です。**通常5〜10分**かかります（12カ国27本のフィードを読み、AIに問い合わせるためフェーズ0より時間がかかります）。

### 実行中に見える4つのステップ

`pipeline` を押すと、中の作業が順に緑になっていきます。名前と意味は次のとおりです。

| 表示名 | 何をしているか |
| --- | --- |
| **①収集（12カ国のRSSを取得）** | `feeds.json` に書かれた27本のフィードを読み、見出しを集めます |
| **②AI付与（日本語訳・要約・タグ・論調）** | 集めた見出しをAIに渡して日本語化します。**キー未登録ならここは「素通り」して緑のまま終わります** |
| **③クラスタ（話題に束ねて採点）** | 同じ話題を束ね、世界TOP30／日本TOP30を作ります |
| **生成データをコミット** | できたデータをリポジトリに保存します（ここでSTEP 5の権限が必要） |

### 成功したか確かめる

1. リポジトリのトップ画面に戻り、**`data`** フォルダを押します。
2. 中に次の4つがあれば成功です。

| 名前 | 中身 |
| --- | --- |
| **`index.json`** | データがある月の一覧（`{"months": ["2026-09"], ...}` のように短いファイル） |
| **`months`** フォルダ | 中に `2026-09.json` のような月ごとのファイル。**日別の状態（ok / stale / empty / failed）**が入っています |
| **`days`** フォルダ | 中に `2026-09-24.json`（集めた見出し）と `2026-09-24.enriched.json`（AI付与後） |
| **`topics`** フォルダ | 中に `2026-09-24.json`。**これが画面に出る話題一覧そのもの**です |

3. **`data` → `topics` → 今日の日付のファイル**を押して、中身を確認します。
   - `"degraded": false` になっていれば、**AI処理が効いています**（キー登録が成功しています）。
   - `"degraded": true` なら、**キーが読めていません**。STEP 7-2 の名前（`GEMINI_API_KEY`）を確認してください。
4. **`data` → `days` → 今日の日付の `.enriched.json`** を押して、`"title_ja"` の隣に**日本語のニュース見出し**が入っていることを確認してください。ここが原語のままなら degraded です。
5. **`data` → `months` → 今月のファイル**を押します。その日の `"status"` が **`"ok"`** になっていれば正常です。
   - `"stale"` は「つながったが記事が古い（配信が止まっている）」
   - `"empty"` は「つながったが記事0件」
   - `"failed"` は「つながらなかった」
   いずれも隠さず記録され、カレンダーに表示されます。

赤い×になった場合は、後半の**トラブルシュート**を見てください。

---

## STEP 9：公開URLを開いて確認する

1. STEP 6で表示されたURL（`https://ユーザー名.github.io/world-lens/`）を開きます。
2. 次が見えていれば**完成**です。

   - 上部に **「世界の視点 / World Lens」** のタイトル
   - **カレンダー**が表示され、今日の日付に件数の数字が付いている
   - **「世界TOP30」** に話題が並び、各話題に**報じた国の国旗や国名が複数**付いている
   - **「日本TOP30」** に日本の媒体が報じた話題が並んでいる
   - 話題を押すと、**日本語の見出し・事実要約・タグ・論調ラベル**と、報じた各国の媒体一覧が開く
   - 媒体名を押すと**元記事**が開く
3. 画面上部に **「⚠ この日のデータにはAI処理が適用されていません（degraded）」** が出ていないか確認します。
   - **出ていなければ成功です。**
   - **出ている場合**はキーが効いていません。トラブルシュート **F** を見てください。
4. URLをブックマークしておきましょう。

**以降は毎朝、自動で最新の話題に更新されます。** 何もしなくて大丈夫です。

---

# トラブルシュート（つまずいたとき）

## A. Actionsが赤い×になる／権限エラー

**症状**：ログに以下のような英語が出る。

```
remote: Permission to xxx/world-lens.git denied to github-actions[bot]
fatal: unable to access ... : The requested URL returned error: 403
```

または

```
Error: Process completed with exit code 128.
```

**原因**：**STEP 5の書き込み権限の設定漏れ**です。これが原因の9割です。

**直し方**：
1. **STEP 5** をもう一度実施します（Settings → Actions → General → Workflow permissions → **Read and write permissions** → Save）。
2. **Actions** タブへ戻り、失敗した行を開いて右上の **「Re-run all jobs」** を押します。

> **注意**：`Re-run all jobs` を押すとAIへのリクエストも再び消費されます。同じ日に何度も押すと 7-4 の1日25リクエストの上限に達します。押すのは**権限を直した後の1回だけ**にしてください。

---

## B. 公開URLを開いたら「404 There isn't a GitHub Pages site here.」

考えられる原因を順に確認します。

1. **待ち時間が足りない**
   → 初回は反映に**最大10分**かかります。**5分待って**再読み込みしてください。
2. **Pagesの設定が保存されていない**
   → STEP 6をやり直します。Branch が `main` かつ `/ (root)` になっているか、もう一度見てください。
3. **URLの末尾が抜けている**
   → `https://ユーザー名.github.io/world-lens/` の**最後のスラッシュまで**必要です。リポジトリ名の部分も忘れずに。
4. **リポジトリが Private になっている**
   → 無料プランでは Public でないと公開できません。Settings の一番下 **「Danger Zone」** → **「Change repository visibility」** で Public に変更します。
5. **`index.html` がリポジトリの一番上の階層にない**
   → リポジトリのトップ画面に `index.html` が見えるか確認します。`phase1/index.html` のように**フォルダの中に入ってしまっている**場合、Pagesは見つけられません。
   **直し方**：`index.html` を押す → 鉛筆マーク（Edit）→ ファイル名の欄を `phase1/index.html` から **`index.html`** に書き換える → Commit changes。
   - 同じことが `feeds.json` `scripts` `.github` にも当てはまります。**すべてトップ階層に置いてください。**

---

## C. ページは出るが「データがまだありません」と表示される

**原因**：`data` フォルダにまだ中身がない（STEP 8の手動実行がまだ、または失敗している）。

**直し方**：
1. **Actions** タブで、緑のチェックが付いた実行があるか確認します。
2. なければ **STEP 8** を実施します。赤い×なら **A** を確認します。
3. `data` フォルダに `index.json`・`months`・`days`・`topics` の**4つ**が揃っているか確認します。
4. 特に **`topics` フォルダ**が無いと画面は空になります。`topics` が無い場合は③クラスタのステップが失敗しています。Actionsのログで **「③クラスタ（話題に束ねて採点）」** を開いて、赤い行のメッセージを読んでください。

---

## D. 画面が真っ白のまま・何も出ない（`file://` で開いた場合）

**症状**：`index.html` をパソコン上で**ダブルクリック**して開いたら、真っ白で何も表示されない。ブラウザのアドレス欄が **`file:///C:/Users/...`** のように **`file://`** で始まっている。

**原因**：**故障ではありません。** ブラウザの安全制限（CORS）です。`file://` で開いたページは、同じフォルダにあるJSONファイルであっても読み込みを禁止されます。`index.html` はデータを別ファイルのJSONから読む作りなので、何も表示できません。

**直し方**：**`https://` で始まるURL（GitHub PagesのURL）から開いてください。** これが唯一の正解です。

- 正しい：`https://ユーザー名.github.io/world-lens/`
- 間違い：`file:///C:/Users/xxx/phase1/index.html`

**どうしても手元で見た目だけ確認したい場合**は、同梱の `sample-data` を使う方法があります（ただしこれも `file://` の制限は受けるので、`https://` 経由が確実です）。

1. `sample-data` の**中身を丸ごと**（`index.json` と `months` `days` `topics` の各フォルダ）、`data` フォルダに**コピー（上書き）**します。
2. 公開URLの末尾に **`?src=sample`** を付けて開きます。
   例：`https://ユーザー名.github.io/world-lens/?src=sample`
   これで `sample-data` 側のデータに切り替わり、**正常日・degraded日・取得失敗日・データ無し日**の4通りの見え方を確認できます。

> **`data` フォルダのJSONをGitHub上でクリックして中身を読む**のも、動作確認としては有効です。ブラウザで表示できなくても、データが正しく作られているかはこれで判断できます。

---

## E. 毎日の自動実行（cron）が遅れる・動かない

**まず知っておいてください。これは故障ではありません。**

1. **GitHubのcronは遅れます。**
   混雑時は**指定時刻から数十分〜数時間遅れて**実行されます。GitHub公式にも「混雑時は遅延することがある」と明記されています。**時刻きっちりには動きません。**
   - 「朝7:10に見たのに更新されていない」のは正常の範囲です。**昼まで待ってから**確認してください。
   - なお `00分` ちょうどは最も混雑するため、あえて `10分` にずらしてあります。

2. **1日2回動くようにしてあります。**
   初期設定では **日本時間の朝7:10 と 当日10:10** の2回実行し、1回目が遅延・スキップされても2回目で回収します。
   - 同じ日に2回動いても**結果は上書きされるだけ**です。記事が重複したり二重に増えたりしません（**冪等**であることを確認済みです）。

3. **★60日ルール★（最大の注意点）**
   リポジトリに**60日間まったく変更（commit）がない**と、GitHubは**自動実行を停止**します。
   - この仕組みは毎日データを保存＝毎日commitが発生するので、**通常は自動的に回り続けます**。
   - ただし**取得が毎日失敗し続けた場合**などは止まる可能性があります。
   - 停止すると、GitHubから **「Scheduled workflow disabled」** という**メールが届きます**。
   - **復活させ方**：Actions タブ → 左の **「daily-pipeline」** を押す → **「Enable workflow」** ボタンを押す。これだけです。その後 **「Run workflow」** を1回押せば即座に再開します。

4. **実行時刻の変更方法**
   `.github/workflows/daily.yml` の `cron` の行を編集します。**GitHubの時刻はUTC（世界標準時）**で、日本時間より**9時間遅れ**です。
   - 日本時間 朝7時10分 → UTC 22時10分（前日）→ `- cron: '10 22 * * *'`（初期設定値）
   - 日本時間 朝8時 → UTC 23時（前日）→ `- cron: '0 23 * * *'`

   編集手順：`.github` → `workflows` → `daily.yml` を押す → 右上の**鉛筆マーク（Edit）** → 該当行を書き換える → **「Commit changes...」**。

---

## F. AI処理が効かない（degraded の警告帯が出たままになる）

**症状**：サイト上部に **「⚠ この日のデータにはAI処理が適用されていません（degraded）。」** が出ている。見出しが英語・中国語などの**原語のまま**並んでいる。

**重要**：Actionsは**緑のチェック（成功）のまま**です。②AI付与のステップは、キーが読めないときも「素通り」して正常終了する作りなので、赤くなりません。**緑だからキーが効いている、とは判断できません。**

原因を上から順に確認してください。

1. **Secretの名前が違う（最も多い原因）**
   → Settings → Secrets and variables → Actions を開き、一覧に **`GEMINI_API_KEY`** と**一字一句同じ名前**があるか確認します。
   - `GEMINI_APIKEY`（アンダースコアが足りない）、`GEMINI_API_KEY `（末尾にスペース）、小文字混じり、はすべて別物として扱われます。
   - 違っていたら、その行の**ゴミ箱マーク（Remove）**で消し、**STEP 7-2** をやり直します。

2. **キーの値に余分な文字が混ざっている**
   → 貼り付けたときに前後に改行やスペースが入った可能性があります。**STEP 7-2 をやり直して上書き登録**してください（中身は確認できないので、作り直すのが確実です）。

3. **キーが無効／失効している**
   → この場合は②AI付与のステップが**赤い×**になります（未登録なら緑のまま degraded、登録済みだが無効なら赤、という違いです）。
   `https://aistudio.google.com/apikey` でキーを**作り直し**、STEP 7-2 で登録し直してください。

4. **その日の無料枠（25リクエスト）を使い切った**
   → 同じ日に手動実行を何度も押した場合に起こります。**翌日まで待てば自動的に戻ります。** 何もしないでください。

5. **キーを登録する前に作られた日のデータを見ている**
   → degraded は**その日のデータに記録された事実**なので、後からキーを登録しても**過去の日付は degraded のまま**です。これは仕様です（欠損を隠さないため）。
   **翌日以降のデータが正常になっていれば問題ありません。** すぐ確認したい場合は STEP 8 の **「Run workflow」** を1回押して、今日の分を作り直してください。

> Secretを登録・変更した後は、**必ず「Run workflow」で1回実行し直してください。** Secretの変更は、すでに終わった実行には反映されません。

---

## G. 特定の国の記事が0件になっている

**症状**：カレンダーや話題一覧で、**ある国だけ記事が1件も出てこない**。あるいは Actionsのログに `⚠ 2媒体を下回った国` のような行が出ている。

**これは想定された状態です。** 報道機関のRSSは予告なく止まったり、URLが変わったりします。27本すべてが永久に生き続けることはありません。

### まず原因を確認する

1. **Actions** タブ → 最新の実行 → **「①収集（12カ国のRSSを取得）」** を押します。
2. ログを上から読み、その国のフィードの行を探します。次のどれかが書かれています。

| ログの表示 | 意味 | 対処 |
| --- | --- | --- |
| `failed` / `HTTP 404` / `HTTP 403` | **URLが変わった・配信が終了した** | 下の「差し替え」または「停止」 |
| `HTTP 429` / `timeout` | **一時的な混雑**。相手のサーバーが忙しい | **何もしない。** 翌日には戻ることが多いです |
| `empty`（0件） | **つながったが記事が0件** | 数日続くなら「停止」 |
| `stale`（鮮度異常） | **つながって記事も返るが、中身が古いまま止まっている** | 「停止」の対象です |

> **`stale` は特に注意してください。** 件数を数えるだけでは見つけられない故障です。大量に記事を返しながら中身が1年前で止まっているフィードがあり、「0件じゃないから大丈夫」という見方では永久に見逃します。そのため**一番新しい記事の日付**で判定しています。

3. **1〜2日で直ることも多い**ので、**まず3日ほど様子を見てください。** 慌てて設定を変える必要はありません。

### 3日以上続く場合：`feeds.json` で止める

**→ 下の「毎日の運用」の「フィードが死んだときの対処」に手順があります。**

---

## H. 話題が束ねられていない（同じニュースが別々に並ぶ）

1. **degraded になっていないか確認します。** → 上の **F** を見てください。翻訳が無いと国をまたいだ束ねは働きません。
2. degraded ではないのに束ねられない場合、**それが実際の報道状況**です。
   実測では、524件の話題のうち**複数の国が報じた話題は28件**でした。**大多数の話題は1カ国だけが報じています。** これは自然な結果で、不具合ではありません。
   - 「世界が同時に注目した話題」は**そもそも少ない**、ということがこのサイトで見える事実のひとつです。
   - 複数国が報じた話題は**世界TOP30の上位に来ます**（国の数 × 3 で採点しているため）。TOP30の上から数件を見てください。

---

# 毎日の運用（何を見れば正常と判断できるか）

**基本方針：毎日何かをする必要はありません。** 月に1〜2回、下の「30秒チェック」をするだけで十分です。

## 1. 30秒チェック（月1〜2回でOK）

公開URL（`https://ユーザー名.github.io/world-lens/`）を開き、**次の4つだけ**見てください。

| # | 見る場所 | **正常** | **異常のサイン** |
| --- | --- | --- | --- |
| 1 | **カレンダー** | **今日か昨日**の日付に件数の数字が入っている | 数字が**3日以上**入っていない → **E**（cron停止）を確認 |
| 2 | **画面上部** | 警告帯が**出ていない** | 「⚠ AI処理が適用されていません（degraded）」→ **F** を確認 |
| 3 | **世界TOP30** | 上位の話題に**2カ国以上**の国名が付いている | 全部1カ国だけ → **F** または **H** を確認 |
| 4 | **見出しの言語** | **日本語**で書かれている | 原語（英語・中国語など）のまま → **F** を確認 |

**この4つが正常なら、それ以上何も確認する必要はありません。**

## 2. もう少し詳しく見たいとき

| 見る場所 | 正常の目安 |
| --- | --- |
| カレンダーの日ごとの件数 | **毎日おおむね同じくらいの件数**（急に半分以下になったら、どこかのフィードが死んでいます） |
| カレンダーの **⚠ マーク** | **出ていない**。出ている日は押すと理由（取得失敗／記事0件）が読めます |
| 話題に付いている**国の数** | 12カ国のうち**10カ国以上**の名前がどこかに出てくる。ある国が丸ごと消えていたら **G** を確認 |
| **Actions** タブ | 一覧が**緑のチェックで並んでいる**。赤い×が続いていたら **A** を確認 |

## 3. GitHubからメールが届いたとき

GitHubは異常時にメールを送ります。**メールが来なければ正常**と考えて構いません。

| メールの件名（英語） | 意味 | 対処 |
| --- | --- | --- |
| **`... workflow run failed`** | その日の実行が失敗した | 1日だけなら**様子見**（相手サーバーの一時的な混雑が多い）。**2日以上続いたら** **A** と **G** を確認 |
| **`Scheduled workflow disabled`** | **60日ルールで自動実行が止められた** | **E-3** の手順で **「Enable workflow」** を押して復活させます |

> 1日失敗しても、翌日の実行で自動的に追いつきます。**1回の失敗で慌てる必要はありません。**

---

# フィードが死んだときの対処（`feeds.json` の `enabled` を `false` にする）

報道機関のRSSは予告なく停止・変更されます。そのときは **`feeds.json` を1文字だけ書き換えて、そのフィードを止めます。** ファイルを削除したり、行ごと消したりはしません。

**なぜ削除せず `false` にするのか**：削除すると「その媒体を調べて、こういう理由で外した」という**履歴が失われます**。数か月後に「この国の媒体を増やしたい」と思ったとき、同じ調査をやり直すことになります。`false` にして残しておけば、**復活させたいときは `true` に戻すだけ**です。

## 手順

1. リポジトリのトップ画面で **`feeds.json`** を押します。
2. 右上の**鉛筆マーク（Edit this file）** を押します。
3. 止めたいフィードを探します。ブラウザの検索（**Windows：`Ctrl` + `F` / Mac：`command` + `F`**）で**媒体名**を入力すると早く見つかります。
   - 媒体名は `"source"` の行に日本語または英語で書かれています（例：`"source": "NHK"`）。
   - Actionsのログに出ていた **`feed_id`**（例：`jp-nhk`）で探しても構いません。
4. その媒体の**かたまり（`{` から `}` まで）**の中にある、次の行を見つけます。

```
      "enabled": true,
```

5. **`true` を `false` に書き換えます。** それだけです。

```
      "enabled": false,
```

   - **`"enabled"` の文字や、前後のカンマ（`,`）は触らないでください。**
   - **`false` は小文字**です。`False` や `FALSE` では動きません。
   - **クォート（`"`）で囲まないでください。** `"false"` は間違いです。
6. できれば、同じかたまりの中の `"note"` の行の末尾に、**止めた理由と日付**を書き添えておきます（任意ですが、後で必ず役に立ちます）。

```
      "note": "（もとの文章）／2026-10-05 HTTP404が3日続いたため停止",
```

7. **★重要★** 右上の緑の **「Commit changes...」** ボタンを押します。
8. 出てきたダイアログで、もう一度緑の **「Commit changes」** を押します。

### 書き換えた後の確認

**`feeds.json` は書き方を間違えると、①収集のステップが丸ごと失敗します。** 必ず確認してください。

1. **Actions** タブ → **「daily-pipeline」** → **「Run workflow」** を1回押します。
2. **緑のチェック**になればOKです。
3. **赤い×**になった場合は、`feeds.json` の書き方が壊れています。ログの **「①収集」** を開いて、次のような行を探してください。

| ログの表示 | 原因 | 直し方 |
| --- | --- | --- |
| `JSONDecodeError` / `Expecting ...` | **カンマや括弧を消してしまった**、`false` をクォートで囲んだ | 手順3〜6をやり直します。心配な場合は、リポジトリの **「History」**（時計マーク）から書き換え前の版を開き、その中身を貼り直せば元に戻せます |
| `feeds.json: ... に必須キー ... がありません` | 行ごと消してしまった | 同上 |
| `feeds.json: feed_id ... が重複しています` | かたまりをコピーして増やした際に `feed_id` が同じになっている | 増やした側の `feed_id` を別の名前に変えます |

### 止めたフィードを復活させるとき

配信が再開したら、同じ手順で **`false` を `true` に戻す**だけです。

### 代わりの媒体を足したいとき

同じ国の別の媒体を足したい場合は、**既存のかたまりを丸ごとコピーして貼り付け**、次の項目を書き換えます。

| 書き換える項目 | 注意 |
| --- | --- |
| `"feed_id"` | **他とかぶらない名前**にします（例：`us-npr`）。かぶると失敗します |
| `"source"` | 画面に出る媒体名 |
| `"country"` | 国コード（`JP` `US` `GB` `FR` `DE` `RU` `CN` `KR` `IN` `BR` `UA` `QA` のいずれか） |
| `"lang"` | その媒体の言語コード（`ja` `en` `fr` `de` `ru` `zh` `ko` `pt` `uk` `ar` など） |
| `"rss_url"` | **RSSのURL**（サイトのURLではありません。多くの報道機関は「RSS」のページで案内しています） |
| `"site_url"` | その媒体のトップページ |
| `"media_type"` | `public`（公営）／`private`（民間）／`state`（国営）／`indep`（独立系）のいずれか。**この4つ以外を書くと失敗します** |
| `"enabled"` | `true` |

貼り付けた後、**必ず「Run workflow」で1回試して緑になるか確認**してください。

> **各国2媒体以上を保つのが目安です。** 1媒体だけになると、その国の報道傾向が偏って見えます。Actionsのログに `⚠ 2媒体を下回った国` と出たら、代わりの媒体を足すことを検討してください。

---

# このフェーズ1でできること・できないこと

**できること**
- **12カ国・27本のフィード**から毎日自動で見出しを取得（実測 **740件**）
- **日本語への翻訳・事実要約・タグ・論調（立場）ラベル**の自動付与
- 同じ話題を束ねて **話題**として整理（実測 **524件**、うち**複数国が報じた話題 28件**）
- **世界TOP30 / 日本TOP30** を「報じた国の数 × 3 ＋ 媒体数」で採点して表示
- カレンダーから日付を選んで過去の日に切り替え
- 見出しから**元記事**へリンク
- 取得に失敗した日を隠さず **「取得失敗」** と表示
- つながったのに記事0件だった日を **「0件」** として区別して表示
- 配信が止まっているフィードを **「⚠ 鮮度異常」** として検出・表示（**件数ではなく一番新しい記事の日付**で判定）
- **APIキーが無くても完走**し、その日を **degraded** として正直に表示
- **スマホ表示に対応**（幅375pxで横スクロールなしを確認済み）
- 月ごとにファイルを分け、**1年運用しても容量が上限を超えない**構造

**まだできないこと（意図的に未実装です）**
- 記事本文の保存（**著作権に配慮し、見出しとリンクのみを保存**しています。**本文系キー0件**を確認済み）
- 話題の時系列追跡（同じ話題が何日続いたかの追跡）
- 過去日のAI付与のやり直し（degraded だった日は degraded のまま残ります）
- キーワード検索、国同士の直接比較ビュー

---

# 免責・運用上の注意

- 本サイトは**個人の非営利**な情報整理を目的としています。
- 見出しと元記事へのリンクのみを扱い、**記事本文は保存・転載していません**。
- 各見出しの**著作権は各報道機関に帰属**します。必ず**出典元の記事**をご確認ください。
- 配信元から削除・停止の要請があった場合は、速やかに対応してください（該当フィードの `enabled` を `false` にします）。
- **AIによる翻訳・要約・論調ラベルは機械的な推定であり、誤りを含みます。** 重要な判断には必ず**原文**を確認してください。論調ラベルは「この媒体の立場はこうだ」と断定するものではありません。
- 取得は**1日1〜2回のみ**で、配信元サーバーに負荷をかけない設計にしています。取得間隔を短くする改造は避けてください。
- **APIキーは他人に渡さないでください。** また、7-4のとおり**無料枠では送信内容が学習に使われ、人間がレビューする場合があります。** 機密情報を扱う用途には使えません。

---

# 用語のミニ辞典

| 言葉 | 意味 |
| --- | --- |
| **リポジトリ** | GitHub上のファイル置き場 |
| **コミット（Commit）** | 変更を保存すること。「保存ボタン」と考えて構いません |
| **Actions** | GitHubが毎日自動でプログラムを動かしてくれる仕組み |
| **ワークフロー** | Actionsに「何をどの順番でやるか」を書いた指示書。今回は `daily.yml` |
| **Pages** | GitHubがファイルをWebサイトとして公開してくれる機能 |
| **Secrets** | GitHubの隠し金庫。**一度入れたら誰にも中身が見えません**。APIキーはここに入れます |
| **APIキー** | AIのサービスを使うための鍵。**パスワードと同じ扱い**をしてください |
| **cron** | 「毎日決まった時刻に動かす」という時刻の設定。**UTC（世界標準時 = 日本時間 − 9時間）**で書きます |
| **RSS** | 報道機関が「新着記事の一覧」を機械用に配信しているファイル |
| **JSON** | データを書くための形式。`{` `}` `"` `,` の書き方が厳密です |
| **degraded** | **機能が落ちた状態**。ここでは「AI処理が適用されていない日」を指します |
| **stale（鮮度異常）** | つながって記事も返るのに、**中身が古いまま止まっている**状態 |
| **冪等（べきとう）** | **何回動かしても結果が同じ**という性質。同じ日に2回動いても記事は二重になりません |
| **クラスタ** | 同じ話題の記事を束ねたかたまり。このサイトの「話題」のこと |
| **CORS** | ブラウザの安全制限。`file://` で開くとデータが読めない原因（トラブルシュート **D**） |
